CRDS_SERVER_URL=https://crds.stsci.edu
CRDS_PATH={{ crds_path | default('') }} # Path to the CRDS cache directory

DATA_ROOT_PATH={{ data_root_path | default('') }} # Root path for data storage
START_STAGE={{ start_stage | default('') }} # Stage to start reduction, e.g., '1b'
